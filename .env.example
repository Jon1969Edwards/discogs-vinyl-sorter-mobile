# Discogs OAuth – copy to .env and fill in your values.
# Never commit .env to version control.
#
# Create an app at: Discogs → Settings → Developers → Create Application
# Use the consumer key and secret for OAuth "Sign in with Discogs" flow.

DISCOGS_CONSUMER_KEY=
DISCOGS_CONSUMER_SECRET=
